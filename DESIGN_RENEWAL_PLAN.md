# Bean-Hoarder — 디자인 리뉴얼 계획

> [이슈 #32](https://github.com/mint-m/Bean-Hoarder/issues/32) 대응 작업 문서.
> 이 문서는 `design/visual-renewal-32` 브랜치의 작업 지침이자 진행 상황 추적 중심점이다.
> **아직 확정된 디자인(색상·타이포 값 등)은 없다** — 이 문서는 틀(양식)과 진행 순서만
> 우선 정의하고, 실제 디자인 값은 각 Phase 진행 중 결정해 채운다.

## 1. 배경 (이슈 #32 요약)

현재 UI(조회 페이지, 덱, `/admin` 랩)가 "AI 생성 느낌"이 강하게 드러나며, 구체적으로:

- 텍스트 가시성 부족 (대비/크기/배치)
- UI 요소 배치가 다듬어지지 않아 정돈된 느낌 부족

## 2. 목표 · 비목표

**목표**: 정보 구조·페이지 흐름·기능은 그대로 두고, 타이포그래피·색상 대비·여백·컴포넌트
배치 등 비주얼 디테일만 다듬어 더 세련되고 깔끔한 톤으로 개선한다.

**비목표 (이번 스코프 밖)**:

- 신규 기능 추가, 정보 구조 재설계, 신규 페이지 추가
- API 계약 변경 (CLAUDE.md 규칙 — 변경 필요 시 이 문서에 먼저 기록 후 별도 합의)
- QR 호환 규칙(KEY 체계, 도메인) 변경

## 3. 범위

| 영역 | 대상 파일 | 비고 |
|---|---|---|
| 조회 페이지 | `public/index.html` | 라이브 트래픽 있음 — QR 스캔 진입점 |
| 덱 | `public/deck.html` | 라이브 트래픽 있음 |
| 공유 스타일 | `public/theme.css`, `public/lab.css` | 디자인 토큰 정의부 |
| 랩(`/admin`) | `apps/lab/` — `components/*.tsx`, `Workspace.tsx`, `AuthView.tsx` 등 | React, 빌드 필요 |
| 라벨 SVG (`packages/label`) | **스코프 제외 대기** | §5-3 결정 후 확정 |

## 4. 진행 상황

> 이 섹션은 각 Phase 완료 시 갱신한다. Phase별로 무엇이 끝났고 무엇이 보류인지를
> 날짜와 함께 한 줄로 남긴다.

- Phase 0 (계획 문서 초안) — ⏳ 진행 중 (이 커밋)
- Phase 1 (디자인 토큰 확정) — 미착수
- Phase 2 (정적 페이지: index/deck) — 미착수
- Phase 3 (랩 `/admin`) — 미착수
- Phase 4 (라벨 SVG 여부 결정 및 반영) — 미착수
- Phase 5 (QA·회귀 확인) — 미착수

## 5. 결정이 필요한 부분 (이슈 원문 + 확장)

이 항목들은 구현 착수 전 사용자 결정이 필요하다. 결정되는 대로 아래에 답을 채워 넣는다.

### 5-1. 우선순위: 어느 화면부터?

- [ ] 조회/덱(라이브 트래픽 있는 정적 페이지)부터 vs. `/admin` 랩부터
- **결정**: _(미정)_

### 5-2. 디자인 토큰: 신규 정의 vs. 기존 다듬기

- [ ] `theme.css`의 기존 CSS 변수 체계(`--bg`, `--surface`, `--ink`, `--accent` 등,
      §6 참고)를 값만 조정 vs. 토큰 구조 자체를 재설계
- **결정**: _(미정)_

### 5-3. 라벨(`packages/label`) SVG 스코프 포함 여부

- [ ] 인쇄물과 묶인 라벨 자체의 시각 디자인도 이번에 손볼지, 완전히 제외할지
- QR 호환 규칙(KEY 체계·도메인)은 어느 쪽이든 불변
- **결정**: _(미정)_

### 5-4. (추가) 레퍼런스 방향성

- [ ] 참고할 구체적 레퍼런스(스타일 무드보드, 유사 서비스 등)가 있는지, 아니면 현재
      "모바일 보딩패스/월렛" 컨셉(theme.css 상단 주석)을 유지한 채 디테일만 개선할지
- **결정**: _(미정)_

## 6. 디자인 토큰 — 현재 상태 스냅샷 (틀만, 값은 미확정)

`public/theme.css`에 정의된 현재 토큰 카테고리. Phase 1에서 이 표를 기준으로
"유지 / 값 조정 / 폐기·대체"를 채워 넣는다.

| 카테고리 | 현재 변수 | 현재 값(라이트) | 처리 방향 |
|---|---|---|---|
| 배경 | `--bg`, `--surface`, `--surface-2` | `#f4f4f5` / `#ffffff` / `#f4f4f5` | _(미정)_ |
| 텍스트 | `--ink`, `--sub` | `#101012` / `#74747c` | _(미정 — 가시성 이슈 핵심 대상)_ |
| 라인 | `--line` | `#e4e4e7` | _(미정)_ |
| 강조색 | `--accent`, `--accent-hover`, `--accent-ink` | `#f04e1c` 계열 (탠저린) | _(미정)_ |
| 상태색 | `--ok`, `--bad` | `#178744` / `#d4341f` | _(미정)_ |
| 서체 | `--mono`, `--sans`, `--display` | Pretendard / Space Grotesk | _(미정)_ |
| 반경 | `--radius`, `--radius-sm`, `--radius-pill` | 16px / 9px / 999px | _(미정)_ |
| 그림자 | `--shadow`, `--shadow-hover` | — | _(미정)_ |
| 다크모드 | `@media (prefers-color-scheme: dark)` 블록 | 라이트와 별도 정의 | _(미정 — 라이트 확정 후 파생)_ |

## 7. 진행 순서 (로드맵 틀)

각 Phase는 §5의 결정 사항이 채워진 뒤 착수한다. 순서는 §5-1 결정에 따라 바뀔 수 있다.

### Phase 0 — 계획 문서 · 브랜치 준비 (이 커밋)

- [x] 이슈 #32 확인, 이 문서 작성
- [x] `design/visual-renewal-32` 브랜치 생성

### Phase 1 — 디자인 토큰 확정

- [ ] §5-2/5-4 결정 반영해 `theme.css` 토큰 값(§6 표) 확정
- [ ] 가시성 이슈(대비) 우선 해결: `--sub`, `--ink` vs. `--bg`/`--surface` 대비비 점검
- [ ] `lab.css`와 `theme.css` 간 중복/불일치 정리

### Phase 2 — 정적 페이지 (index / deck)

- [ ] `public/index.html` 레이아웃·타이포 조정
- [ ] `public/deck.html` 레이아웃·타이포 조정
- [ ] 두 페이지 모두 라이트/다크 모드 육안 확인

### Phase 3 — 랩 (`/admin`)

- [ ] `apps/lab/src/components/*.tsx` 개별 컴포넌트 스타일 조정
- [ ] `Workspace.tsx`, `AuthView.tsx` 레이아웃 조정
- [ ] `npm run build -w @bnhd/lab` 후 `wrangler pages dev`로 `/admin` 육안 확인

### Phase 4 — 라벨 SVG (§5-3 결정 시)

- [ ] 포함 결정 시: `packages/label` 렌더러 시각 조정, QR 검증 테스트 통과 확인
- [ ] 제외 결정 시: 본 섹션 스킵 처리하고 이 문서에 명시

### Phase 5 — QA · 회귀 확인

- [ ] `npm run check` (lint + typecheck + test) 통과
- [ ] 조회/덱/랩 라이브 흐름 수동 확인 (로그인, 등록, 조회, 덱 카드)
- [ ] 라이트/다크 모드 각각 확인
- [ ] `main` 푸시 전 스크린샷 비교(변경 전/후) 정리

## 8. 원칙 (이슈 원문 기반)

- 큰 구조 변경(정보 구조 재설계, 신규 페이지 추가)은 스코프 밖 — 순수 비주얼 리뉴얼.
- API 계약/QR 호환 규칙(CLAUDE.md)은 이번 작업과 무관하지만, 화면 노출 데이터 포맷을
  건드리게 될 경우 유의.
- 각 Phase 착수 전 §5 관련 결정 항목이 채워졌는지 먼저 확인한다.
