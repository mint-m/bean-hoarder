# Bean-Hoarder — 디자인 리뉴얼 계획

> [이슈 #32](https://github.com/mint-m/Bean-Hoarder/issues/32) 대응 작업 문서.
> 이 문서는 `design-system` 브랜치의 작업 지침이자 진행 상황 추적 중심점이다.
> **디자인 시스템(방향·규칙)은 [DESIGN.md](DESIGN.md)로 확정**됐고, 토큰 값의 단일 소스는
> `apps/web/public/theme.css`다. 이 문서는 그 시스템을 화면에 적용해 가는 **다단계 리뉴얼의
> 진행 추적**을 맡는다 — Phase마다 무엇이 끝났는지 여기 갱신한다.

## 1. 배경 (이슈 #32 요약)

현재 UI(조회 페이지, 덱, `/lab` 랩)가 "AI 생성 느낌"이 강하게 드러나며, 구체적으로:

- 텍스트 가시성 부족 (대비/크기/배치)
- UI 요소 배치가 다듬어지지 않아 정돈된 느낌 부족

## 2. 목표 · 비목표

**목표**: 정보 구조·페이지 흐름·기능은 그대로 두고, 타이포그래피·색상 대비·여백·컴포넌트
배치 등 비주얼 디테일만 다듬어 더 세련되고 깔끔한 톤으로 개선한다.

**비목표 (이번 스코프 밖)**:

- 신규 기능 추가, 정보 구조 재설계, 신규 페이지 추가
- API 계약 변경 (CLAUDE.md 규칙 — 변경 필요 시 이 문서에 먼저 기록 후 별도 합의)
- QR 호환 규칙(KEY 체계, 도메인) 변경

## 3. 범위

| 영역 | 대상 파일 | 비고 |
|---|---|---|
| 조회 페이지 | `apps/web/index.html` | 라이브 트래픽 있음 — QR 스캔 진입점 |
| 덱 | `apps/web/deck.html` | 라이브 트래픽 있음 |
| 공유 스타일 | `apps/web/public/theme.css`, `apps/web/public/lab.css` | 디자인 토큰 정의부 |
| 랩(`/lab`) | `apps/lab/` — `components/*.tsx`, `Workspace.tsx`, `AuthView.tsx` 등 | React, 빌드 필요 |
| 라벨 SVG (`packages/label`) | **스코프 제외 대기** | §5-3 결정 후 확정 |

## 4. 진행 상황

> 이 섹션은 각 Phase 완료 시 갱신한다. Phase별로 무엇이 끝났고 무엇이 보류인지를
> 날짜와 함께 한 줄로 남긴다.

- Phase 0 (계획 문서·브랜치) — ✅ 완료
- Phase 1 (디자인 시스템 확립 + 토큰 확정) — ✅ 완료 (2026-08-17): DESIGN.md 작성, `--sub` 대비 AA 상향,
  여백 스케일·커피 컬러 훅 신설. 상세는 §7 Phase 1.
- Phase 2 (정적 페이지: index/deck 적용) — ✅ 완료 (2026-08-17): 1차(스트립 은퇴·accent 정리)에
  이어 2차 **레이아웃 재설계** — 커피 컬러 엔진 신설(`apps/web/src/lib/coffee-color.ts`: 산지 OKLCH
  시그니처 + 향미 무드 그라데이션), 상세 카드를 항공권 3단 구조(아이덴티티 밴드/티켓 정보 행/스텁)로,
  덱을 피크 밴드 구조로 재편. 반경 토큰 축소(16→10, 9→7).
- Phase 3 (랩 `/lab`) — ✅ 1차 "작업대" 재편 (2026-08-17) → ✅ **2차 전면 재설계** (2026-08-19).
  1차는 2열 작업대였으나 여전히 난잡했고, 무엇보다 **서비스 목표가 재정의**됐다 — 라벨을 만들어 주는
  것이 아니라 **① 최적화된 정보 입력 ② 인쇄에 최적화된 QR ③ 유지관리**. 그래서 3막 단일 컬럼으로
  다시 세웠다. 상세는 §7 Phase 3.
- Phase 4 (라벨 SVG) — **제외 확정** (§5-3)
- Phase 5 (QA·회귀 확인) — ✅ 완료 (2026-08-17): `npm run check` 통과(99 테스트) + **e2e 10건 통과**.
  재편 과정에서 잡은 회귀 3건: ① 랩 우측 열에 로고 관리가 오면서 `.status-line`이 복수가 돼 e2e
  셀렉터가 모호해진 것 → 등록 상태줄에 `.register-status` 식별 클래스 부여, ② 덱 피크 밴드에서
  flex 컨테이너에 걸린 말줄임이 동작하지 않던 것 → 내부 텍스트 span으로 이동, ③ 덱 카드 고정 높이가
  노트 표시 시 본문 3행을 자르던 것 → 140px→158px. 상세·덱·랩 라이트·다크 육안 확인 완료.

## 5. 결정 사항 (확정)

### 5-1. 우선순위: 어느 화면부터?

- **결정**: **조회/덱(라이브 정적 페이지) 먼저 → 랩 다음.** 커피 컬러가 먼저 붙는 상세(조회)가 1순위.

### 5-2. 디자인 토큰: 신규 정의 vs. 기존 다듬기

- **결정**: **기존 CSS 변수 체계 유지(다듬기+확장)** — 구조 재설계 아님. 값 조정(`--sub` 대비)과
  신규 토큰(여백 스케일 `--sp-*`, 커피 컬러 훅 `--coffee-strip`)만 추가.

### 5-3. 라벨(`packages/label`) SVG 스코프 포함 여부

- **결정**: **제외.** 라벨은 감열 인쇄·흑백/2도(`#e8341c`)에 고정이라 이번 화면 리뉴얼에서 뺀다.
  QR 호환 규칙(KEY 체계·도메인)은 불변. 커피 컬러도 **화면 전용** — 라벨엔 색이 없다.

### 5-4. 레퍼런스 방향성

- **결정**: **모던·미니멀(Ollama류 절제) + 커피 컬러 포인트.** 중립 크롬을 유지하고 다양성 색은
  원두에서 — 국가=시그니처 모노컬러, 향미=무드 그라데이션(화면 전용, 지금은 상세 페이지만).
  전체 규칙은 [DESIGN.md](DESIGN.md).

## 6. 디자인 토큰 — Phase 1 처리 결과

`apps/web/public/theme.css` 토큰의 Phase 1 처리. 값의 단일 소스는 theme.css이며, 규칙은 [DESIGN.md](DESIGN.md).

| 카테고리 | 변수 | 값(라이트) | 처리 |
|---|---|---|---|
| 배경 | `--bg`, `--surface`, `--surface-2` | `#f4f4f5` / `#ffffff` / `#f4f4f5` | 유지 |
| 텍스트 | `--ink`, `--sub` | `#101012` / **`#6a6a72`** (구 `#74747c`) | **`--sub` 상향** — `--bg` 위 4.2:1 → 4.9:1(AA). `--ink` 유지 |
| 라인 | `--line` | `#e4e4e7` | 유지 |
| 강조색 | `--accent`, `--accent-hover`, `--accent-ink` | `#f04e1c` 계열(탠저린) | 유지 — 원두 상세에선 산지 색으로 로컬 대체(DESIGN.md §2·§3) |
| 상태색 | `--ok`, `--bad` | `#178744` / `#d4341f` | 유지 |
| 서체 | `--mono`, `--sans`, `--display` | Pretendard / Space Grotesk | 유지 |
| 반경 | `--radius`, `--radius-sm`, `--radius-pill` | 16 / 9 / 999px | 유지 |
| 여백 | **`--sp-1`…`--sp-7`** | 4·8·12·16·24·32·48px | **신설** — 컴포넌트 간격 스케일 |
| 커피 컬러 | **`--coffee-strip`** | `var(--accent)` 폴백 | **신설(훅)** — 상세 산지 색 자리, 엔진은 Phase 2 |
| 그림자 | `--shadow`, `--shadow-hover` | — | 유지 |
| 다크모드 | `prefers-color-scheme` 블록 | 라이트와 별도 정의 | 유지 — 다크 `--sub`은 이미 AA<br>(3차에서 `:root[data-theme="dark"]` opt-in으로 전환) |

## 7. 진행 순서 (로드맵 틀)

각 Phase는 §5의 결정 사항이 채워진 뒤 착수한다. 순서는 §5-1 결정에 따라 바뀔 수 있다.

### Phase 0 — 계획 문서 · 브랜치 준비 (이 커밋)

- [x] 이슈 #32 확인, 이 문서 작성
- [x] `design-system` 브랜치를 origin/main에서 분기 (구 `design/visual-renewal-32`는 비어 있어 폐기)

### Phase 1 — 디자인 시스템 확립 + 토큰 확정 ✅ (2026-08-17)

- [x] [DESIGN.md](DESIGN.md)를 비주얼 디자인 시스템으로 재작성 (아이덴티티·색·커피 컬러·타이포·여백·컴포넌트)
- [x] 아키텍처 내용은 README/HOW_IT_WORKS로 정리하고 DESIGN.md에서 제거 (README·CLAUDE.md 문서표 갱신)
- [x] 가시성: 라이트 `--sub` `#74747c` → `#6a6a72` (AA ≥4.5:1)
- [x] 신규 토큰: 여백 스케일 `--sp-*`, 커피 컬러 훅 `--coffee-strip`
- [ ] (후속) `lab.css`와 `theme.css` 간 중복/불일치 정리 — 화면 적용 Phase에서

### Phase 2 — 정적 페이지 (index / deck) + 커피 컬러 엔진

AI느낌의 원인을 [DESIGN.md](DESIGN.md) §1의 anti-AI 표로 규칙화하고, 그 기준으로 정리했다.

- [x] **풀블리드 컬러 스트립 은퇴** — 상세 `.pass`(6px)·덱 `.wcard`(5px) 제거, 경계는 헤어라인으로.
      산지 색은 **작은 스와치/닷 하나**로만 (상세: 로스터리 줄, 덱: 헤드라인 옆)
- [x] accent 장식 사용 정리 — 로스터리 eyebrow·`D+N` 알약·랜딩 보조 링크·스캔 버튼을 중립으로
      (accent는 주 버튼·본문 링크·포커스에만)
- [x] 타이포 정돈 — 헤드라인 크기·자간 축소, 마이크로 라벨 자간 완화, 칩 보더 완화
- [x] 헤드라인 줄바꿈에서 `·`가 줄 첫머리에 떨어지지 않게 (표시 전용, 공유 규칙 불변)
- [x] 조회 라이트/다크 + 덱 육안 확인, `npm run check` 통과

**2차 (2026-08-17): 스타일 조정을 넘어 레이아웃 재설계** — "단순 색·공백 변경은 리뉴얼이 아니다"(사용자
피드백)에 따라 구조를 재편했다.

- [x] **커피 컬러 엔진** `apps/web/src/lib/coffee-color.ts` 신설 (구 산지 색 모듈 대체) —
      산지 OKLCH 시그니처(큐레이션 hue + 해시 폴백) + 향미 무드 그라데이션 (DESIGN.md §3)
- [x] **상세 카드를 항공권 구조로 재편** — 아이덴티티 밴드(향미 그라데이션+스와치+헤드라인) /
      티켓 정보 행(RSTD·PKGD·NET·ROAST PT) / 스텁(KEY·D+N·원본) (DESIGN.md §7)
- [x] **덱을 피크 밴드 구조로 재편** — 카드 높이 고정, 스택시 보이는 띠에 그라데이션·닷·헤드라인 집약
- [x] 반경 토큰 축소 16/9 → **10/7** (모던 톤), 임의 반경 잔재를 토큰으로 통일

### Phase 3 — 랩 (`/lab`) — ✅ 전면 재설계 (2026-08-19)

**서비스 목표 재정의가 출발점이다.** 라벨 레이아웃은 쓰는 사람마다 다르고 프린터 호환도 보장할 수
없다 → 서비스가 책임지는 것은 **① 최적화된 정보 입력 ② 인쇄에 최적화된 QR ③ 유지관리**이고,
라벨 도안은 선택 부가물로 내린다. 화면을 그 순서 그대로 3막으로 세웠다.

- [x] **1막 입력** — [IntakeCard](apps/lab/src/components/IntakeCard.tsx): 링크 하나로 AI 초안 생성이
      진입점(붙여넣기·AI키는 접힘), 인식되면 접힌 한 줄로 물러난다.
      [ReviewStepper](apps/lab/src/components/ReviewStepper.tsx): 등록 필수 순서로 6스텝, 한 번에 하나만
      열림, **건너뛰기 허용**, 완료 스텝은 요약 한 줄. AI가 채운 칸엔 "확인" 표시.
- [x] **추천 칩으로 타이핑 최소화** — [suggest.ts](apps/lab/src/lib/suggest.ts)에 산지·가공·품종·
      로스팅포인트·수확·용량·플레이버 풀 + 날짜 칩(오늘/어제/3일 전…). 칩은 datalist와 같은 소스를 쓴다.
- [x] **2막 QR 발급** — [QrCard](apps/lab/src/components/QrCard.tsx). `buildQrSVG`(packages/label)로
      203dpi 도트 정수배 모듈 + 최소 여백(2모듈), 크기 3종(9.4/12.5/15.6mm), PNG·SVG,
      URL·이미지 복사. **등록 전에는 내보내지 않는다**(예상 KEY QR은 없는 곳을 가리킨다).
      QR 검증(`verifyQr`)도 라벨이 아니라 이 QR을 본다 — 타이핑마다 돌던 검증이 KEY·크기 변경 시에만 돈다.
- [x] **라벨 강등** — [LabelPanel](apps/lab/src/components/LabelPanel.tsx) 접힌 패널로 이동(사이즈·미리보기·
      다운로드 + DesignCard 중첩). 기능 손실 0. 좌우 2열·모바일 FAB 시트는 폐지(단일 컬럼이라 불필요).
- [x] **복사 피드백** — `CopyButton`([FormBits](apps/lab/src/components/FormBits.tsx)): 누른 자리에서
      `✓ 복사완료` 1.5초. QR·목록·복구키의 복사 버튼을 하나로 통일.
- [x] BeanFormCard·PreviewCard 삭제, lab.css를 3막 플로우로 재작성(단일 컬럼 640px)
- [x] 검증: `npm run check` 통과(103 테스트 — QR 도트 정합 4건 추가) + **e2e 10건 통과**(새 동선으로 갱신)

**3차 (2026-08-24): 사용성 피드백 반영 + 주소 정리.** 실제로 써 보고 나온 지적을 처리했다.

- [x] **자동 채우기가 "되다 안 되다" 하던 버그** — `setForm(updater)` 안에서 결과를 모으고 곧바로
      성공을 판정했는데, React는 대기 중인 업데이트가 없을 때만 updater를 즉시 실행한다(eager state).
      직전에 다른 상태 변경이 있으면 미뤄져 "이미 다 입력돼 있습니다"가 뜨며 멈췄다 →
      다음 폼을 **setForm 바깥에서 순수하게 계산**하도록 고침. 실패 문구도 원인별로 분리.
- [x] **인테이크 입력 칸 통합** — 링크와 본문을 한 칸에서 받는다(링크면 페이지를 가져오고, 아니면 본문을 읽음).
      우회로였던 붙여넣기 UI가 통째로 사라졌다.
- [x] **URL 선(先) 중복 확인** — 같은 상품 페이지로 등록한 원두가 있으면 가져오기 **전에** 묻는다.
      목록이 이미 메모리에 있어 추가 요청 없이 fetch·AI 호출을 통째로 아낀다. 등록 시점의
      로스터리·산지·로스팅일 확인은 두 번째 그물로 남긴다.
- [x] **점진적 노출·설정 서랍** — 붙여넣기·AI 키를 본 화면에서 제거(설정은 우측 상단 서랍으로),
      추천 칩은 자주 쓰는 것만 + `+N개 더`, 카메라 버튼은 터치 기기에서만 아이콘으로.
- [x] **화면 분리** — 입력/QR/라벨/목록을 한 번에 하나만 보이게 하고 브라우저 뒤로가기를 지원.
- [x] **테마** — OS 다크 추종 폐지, 라이트 고정 + 설정에서 다크 선택(`:root[data-theme="dark"]`).
- [x] **QR 정리** — 문구 평이화(`✓ 인쇄해도 스캔됩니다`), 버튼 6→4(주소/인쇄물로 묶음),
      여백 4→**2모듈**(라벨의 흰 바탕이 나머지를 대신한다는 전제 — 3가지 크기 모두 실디코드 확인).
- [x] **덱 → 수정 진입** — 액션 시트에 "정보 수정", 랩을 `?edit=KEY`로 열어 편집 상태 로드.
- [x] **덱 밴드 페이드** — 밴드/본문 경계 12px을 본문 색으로 풀어 딱 잘리는 느낌 제거.
- [x] **중복 원두 경고** — 로스터리+산지+로스팅일이 같으면 등록 전 확인(소분 재등록은 그대로 진행).
- [x] **DEMO 읽기 전용 + TEST 분리** — 공개된 자격증명이라 서버가 쓰기를 403으로 막고(`writeAllowed`),
      e2e는 시드의 TEST 계정을 쓴다. 랩도 데모에선 등록 UI 대신 가입 안내를 보여준다.
- [x] **`/admin` → `/lab`** — 관리자 화면이 아니라 등록·QR 발급 화면이므로 서비스가 부르는 이름과 맞췄다.
      `/admin`은 제거(남은 200 응답은 QR 계약의 근거인 조회 페이지 폴백). QR 계약 e2e로 회귀 없음 확인.
- [x] 검증: `npm run check` 통과(105 테스트 — 데모 쓰기 차단 계약 2건 추가) + **e2e 10건 통과**

### Phase 4 — 라벨 SVG — **제외 확정** (§5-3)

- 라벨은 감열 인쇄(흑백/2도)에 고정 — 이번 리뉴얼 스코프에서 제외한다. 커피 컬러는 화면 전용.

### Phase 5 — QA · 회귀 확인 ✅ (2026-08-24)

- [x] `npm run check` 통과 — 105 테스트(단위 + workerd/D1 통합), lint·typecheck·문서 참조 검증
- [x] `npm run e2e` 10건 통과 — QR 라우팅 계약 6건 포함(주소 변경 후에도 인쇄된 라벨 경로 무사)
- [x] 조회·덱·랩 실제 브라우저 확인 (로그인, 인테이크 두 경로, 스텝 확인/건너뛰기, 등록, QR 발급,
      덱→수정, 복사 피드백, 데모 읽기 전용)
- [x] 라이트/다크 각각 확인 — OS가 다크여도 앱은 라이트 유지, 설정 토글로 즉시 전환
- [x] 대비 수치 확인 — 라이트 `--sub` 4.88:1, 다크 6.09:1 (AA ≥4.5:1)

## 8. 원칙 (이슈 원문 기반)

- 큰 구조 변경(정보 구조 재설계, 신규 페이지 추가)은 스코프 밖 — 순수 비주얼 리뉴얼.
- API 계약/QR 호환 규칙(CLAUDE.md)은 이번 작업과 무관하지만, 화면 노출 데이터 포맷을
  건드리게 될 경우 유의.
- 각 Phase 착수 전 §5 관련 결정 항목이 채워졌는지 먼저 확인한다.
