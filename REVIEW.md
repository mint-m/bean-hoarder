# Bean-Hoarder 서비스 종합 평가 보고서

> 작성일: 2026-07-08 · 평가 대상: `main` 브랜치 (커밋 `1f8aeec`, v2 라이브 기준)
> 관점: **① 서비스 사용자** (원두 호더로서 가입→등록→인쇄→스캔 전 과정), **② 외부 개발자** (코드베이스를 처음 받아 읽고 기여·운영한다는 가정)
> 방법: 전체 소스·문서 정독 및 정적 분석. 라이브(`bnhd.pages.dev`) 실접속 검증은 평가 환경의 네트워크 정책상 수행하지 못함.

---

## 1. 총평

**"개인 취미 도구가 도달할 수 있는 완성도의 상한선에 가까운 프로젝트."**
문제 정의(여러 로스터리 원두를 소분·보관하는 개인의 라벨링·조회)가 명확하고, 그 규모에 딱 맞는 기술 선택(Cloudflare Pages + Functions + D1, 운영비 0원)을 했으며, 무엇보다 **모든 설계 결정에 근거가 문서로 남아 있다.** v1의 마찰 → v2의 해법을 표로 정리한 DESIGN.md, 수용한 트레이드오프의 명시, 진행 기록까지 — 1인 프로젝트에서 보기 드문 수준이다.

반면 의도적으로 수용한 보안 모델(4자리 숫자 암호, 레이트 리밋 없음)은 문서화된 전제("암호는 탈취돼도 무방")보다 실제 노출 면이 한 단계 넓고, 저장소에 초대코드가 평문 커밋되어 있으며, 테스트·라이선스·로컬 개발 가이드가 없어 "외부 개발자가 합류하는" 시나리오에는 아직 준비가 덜 되어 있다.

| 평가 항목 | 평점 (5점) | 요약 |
|---|---|---|
| 문제 정의·제품 적합성 | ★★★★★ | 니치가 분명하고 스코프 통제가 탁월 |
| 사용자 경험 (등록·인쇄·조회) | ★★★★☆ | 자동채우기·실시간 QR 검증 등 디테일 우수. 라벨 디자인 설정 비영속 등 소소한 마찰 |
| 아키텍처 | ★★★★★ | 규모 대비 과잉도 부족도 없음. 비용 0원 구조 검증됨 |
| 코드 품질 | ★★★★☆ | 의존성 최소, 주석에 "왜"가 담김. 단일 파일 비대화 조짐 |
| 보안 | ★★☆☆☆ | 위협 모델은 정직하나 PIN 무차별 대입·초대코드 커밋 등 실질 구멍 존재 |
| 문서화 | ★★★★★ | README/DESIGN/HOW_IT_WORKS/PROJECT 4중 구조, 의사결정 기록 모범 |
| 기여 용이성 (외부 개발자) | ★★☆☆☆ | 테스트 0, CI 0, LICENSE 없음, 로컬 개발 절차 미문서화 |

---

## 2. 서비스 개요 (분석 기반 재구성)

- **무엇**: 40×20mm 님봇(NIIMBOT) 라벨에 원두 정보+QR을 인쇄하고, 스캔하면 `bnhd.pages.dev/{KEY}`에서 상세 카드를 보여주는 서비스. KEY 체계는 `{유저코드4}{연도2}-{순번3}` (예: `BXNQ26-001`).
- **누구를 위해**: 여러 로스터리에서 원두를 사 모아 소분·보관하는 "원두 호더" 개인. 초대코드 기반 폐쇄형(지인 소모임 규모).
- **구성** (Cloudflare Pages 프로젝트 1개가 전부):
  - `v2/public/` — 조회(`index.html`), 스튜디오(`admin.html`, LAB), 월렛 덱(`deck.html`), 라벨 렌더러(`label.js`), 자동채우기 파서(`autofill.js`), 공용 테마(`theme.css`)
  - `v2/functions/api/[[path]].js` — 단일 라우터: signup / recover / beans CRUD / 공개 조회 / CSV 백업 / 링크 가져오기 프록시
  - D1(`bnhd-v2`) — `users(usercode, pass_hash, recovery_hash)` + `beans(key, usercode, 원두 필드 17개)`
- **코드 규모**: v2 전체 약 2,400줄 (서버 325줄). 빌드 도구·프레임워크·npm 의존성 없음(런타임 CDN 2개: qrcode-generator, jsQR).

---

## 3. 사용자 관점 평가

### 3.1 잘 만든 것

**등록 흐름의 마찰 제거가 집요하다.** 스튜디오(LAB)의 입력 편의 장치들이 실사용 시나리오("로스터리 상품 페이지를 보며 소분 직후 등록")에 정확히 맞춰져 있다:

- **자동 채우기**: 상품 페이지 URL을 넣으면 서버 프록시가 텍스트를 추출하고, 휴리스틱 파서가 산지·가공·품종·고도·수확시기·용량·노트를 인식해 빈 칸만 채움. "이미 입력된 항목은 덮어쓰지 않고, 결과는 반드시 검토"라는 원칙이 UI 문구에 명시됨. 에티오피아 JARC 품종코드(74xxx), "December 2025 – January 2026" 같은 수확 월-범위 정규화까지 처리하는 디테일.
- **실시간 라벨 미리보기 + QR 실검증**: 입력할 때마다 라벨 SVG가 다시 그려지고, 203dpi로 래스터화한 이미지를 jsQR로 **실제 디코드**해 "QR 검증 OK" 배지를 띄운다. 인쇄 실패를 인쇄 전에 잡는 구조 — 이 서비스의 핵심 신뢰 장치이며 매우 잘 설계됨.
- 날짜 기본값(로스팅일 D-10, 패키징일 오늘), 용량·고도 단위 자동 부착, 로스팅 포인트 `#` 자동 접두, 산지·가공 datalist 제안(블렌드 포함), 자주 안 쓰는 필드(고도·생산자·랏 등) 접기, 서브라인 값 입력 시 라벨 표시 자동 체크 + 스펙 칸과 중복 방지 상호 배타 처리.
- **모바일 대응**: 879px 이하에서 미리보기가 FAB + 바텀시트로 전환. 다크 모드 완비.

**등록 후 동선이 현실적이다.** "상세 URL 복사 / QR 이미지 클립보드 복사"가 1차 동선이고 라벨 PNG(203/320dpi)·SVG 다운로드는 보조 — 님봇 앱에 이미지를 넘기는 실제 인쇄 절차와 맞다. 클립보드 미지원 브라우저는 다운로드로 폴백.

**조회 페이지가 서비스의 얼굴 값을 한다.** 보딩패스 컨셉 카드, 테이스팅 노트를 짧으면 칩·길면 문단으로 분기, 빈 필드 자동 숨김, 로스팅일 기준 **D+N 신선도 배지**, 코드 없이 접속 시 랜딩(코드 직접 입력 + 데모 링크). 오류 상태(형식 오류/미등록/로드 실패) 처리도 빠짐없다. 덱(`/deck`)의 월렛 카드 스택 + 통합 검색은 "내가 모은 원두를 훑는" 호더의 재미 요소를 정확히 짚었다.

**데이터 소유권 안전장치**: CSV 백업(BOM 포함 — 엑셀 한글 정상), 복구키(가입 시 1회 표시, 저장 확인 체크 강제, 사용 시 회전)는 이 규모 서비스로는 성실한 설계다.

### 3.2 사용자로서 아쉬운 것

| # | 항목 | 내용 | 체감 |
|---|---|---|---|
| U1 | **라벨 디자인 설정이 저장 안 됨** | `design` 객체와 업로드한 로고가 메모리에만 있어 새로고침하면 기본값으로 리셋. 로고를 매 세션 다시 업로드해야 함 | 반복 사용자일수록 큼. localStorage 저장이면 해결 |
| U2 | **메모가 공개 페이지에 노출** | "재구매 의사, 추출 레시피 등 자유롭게"라고 안내되는 메모 필드가 QR 스캔한 **누구에게나** 보임. 라벨을 지인에게 원두와 함께 건네는 사용 패턴을 생각하면, 사적 메모("이 집은 비쌈" 류)가 의도치 않게 공유될 수 있음 | 입력란에 "공개 표시됨" 안내 또는 비공개 메모 분리 필요 |
| U3 | **암호 변경 수단이 복구키뿐** | 일반적인 "암호 변경" 기능이 없어 PIN을 바꾸려면 복구키를 소모(회전)해야 함 | 낮음, 그러나 직관과 어긋남 |
| U4 | **계정 = 복구키 단일 생명선** | 유저코드+PIN과 복구키를 모두 잃으면 영구 손실(이메일 없음). 폐쇄형이라 운영자가 수동 구제할 수는 있으나 절차 없음 | 문서에 경고는 충실함 |
| U5 | 삭제→재등록 시 번호 재사용 | 최신 KEY 삭제 후 재등록하면 같은 번호가 부여됨. **이미 인쇄한 라벨**의 QR이 다른 원두를 가리킬 수 있음 | DESIGN.md에 의도로 기록됨. 인쇄 후 삭제 시 경고문이 이미 있어 완화됨 |
| U6 | 오프라인 취약 | 폰트·QR 라이브러리가 CDN 의존이라 오프라인/차단 환경에서 스튜디오가 동작 불가 | 낮음 |
| U7 | 목록 기능 최소 | 정렬(KEY순 고정)·필터는 덱에서만, 스튜디오 목록은 검색 없음. 원두 수십 개까지는 문제없음 | 낮음 |

**사용자 관점 결론**: 초대받은 사용자가 겪는 핵심 루프(등록→인쇄→스캔)는 상용 서비스 수준으로 다듬어져 있다. U1·U2가 실사용에서 가장 먼저 부딪힐 마찰.

---

## 4. 외부 개발자 관점 평가

### 4.1 첫인상: 문서가 코드보다 먼저 온보딩을 끝낸다

README(현재 상태·운영법) → DESIGN.md(v1 마찰→v2 해법 표, 수용한 트레이드오프) → HOW_IT_WORKS.html(그래픽 설명) → PROJECT.md(v1 기록 보존)의 4중 구조가 역할 분담이 명확하다. 특히 **"v1 대비 잃는 것"을 표로 남긴 것**, 마이그레이션 SQL 4개를 지우지 않고 보존한 것, 커밋 메시지가 변경 의도를 담는 것 — 히스토리 고고학 없이 현재 설계에 도달한 이유를 전부 추적할 수 있다. 오픈소스 대형 프로젝트에서도 드문 미덕.

사소한 문서 드리프트: DESIGN.md의 API 목록에 이후 추가된 `/api/recover`·`/api/fetch`가 빠져 있고, README 진행기록의 "필수 항목 …·용량" 문구는 이후 커밋(`e949c9c`)에서 용량이 필수 해제된 것과 어긋난다(기록 성격이라 치명적이진 않음).

### 4.2 아키텍처: 규모에 정확히 맞는 옷

- 서버가 단일 파일 325줄. 라우팅→인증→핸들러가 한눈에 들어오고, 프레임워크·ORM 없이 D1 prepared statement 직접 사용 — 이 규모에서 올바른 선택.
- **KEY 채번**: `MAX(순번)+1` 계산 후 INSERT, PK 충돌 시 3회 재채번. 낙관적 동시성 처리로 단일 사용자 도구에 충분하며 한계가 주석에 명시됨.
- **비용 모델 검증**: DESIGN.md의 무료 한도 대비 사용량 표(수천 배 여유)는 실제 D1/Functions 무료 티어와 부합. "0원" 주장은 근거가 있다.
- 렌더러 단일화(label.js 하나로 미리보기·다운로드·검증)는 v1의 Python/JS 이중화 문제를 정확히 해소. 다만 `tools/make_label.py`(199줄)가 배치 인쇄용으로 남아 있어 라벨 사양 변경 시 **두 곳을 고쳐야 하는 이중화가 사실상 부활**한 상태 — v2 라벨이 보딩패스 디자인으로 크게 바뀐 지금, make_label.py는 v1 디자인을 그대로 갖고 있다(README는 "그대로 사용 가능"이라 하지만 산출물 디자인이 다름).

### 4.3 코드 품질

**강점**
- 주석이 "무엇"이 아니라 "왜"를 설명한다: 도트 격자 스냅(0.125mm)과 실도트 피치의 0.1% 오차 허용, 한글 폭 1.8배 계산 근거, REGION만 콤마 병합하는 파서 예외의 제품 결정 배경 등.
- XSS 방어 일관성: 사용자 입력이 DOM에 들어가는 경로(`index.html` 렌더, 목록, 덱 카드)에서 `escapeHtml` 또는 `textContent` 사용을 확인함. KEY는 서버에서 정규식(`KEY_RE`)으로 형식이 강제되어 href 삽입도 안전.
- SQL은 전부 바인딩 파라미터. 컬럼명 동적 조립(`FIELDS.join`)은 상수 배열 기반이라 인젝션 벡터 아님.
- 라벨 엔진의 플로우 레이아웃(내용량에 따라 정보 블록 줄 수를 4→1로 역산해 바닥 안에 수납)과 `textLength` 고정에 의한 넘침 방지는 40×20mm라는 극한 제약을 우아하게 다룬다.

**약점**
- `admin.html`이 1,016줄 단일 파일(스타일+마크업+로직). label.js·autofill.js는 분리됐지만 계정·목록·저장·클립보드 로직이 한 파일에 눌러 담겨 있어, 다음 기능 추가부터 비용이 가팔라질 변곡점.
- 프론트 상태가 전역 변수(`mode`, `confirmedKey`, `previewSeq`, `window.__svg`) — 현재 규모에선 동작하나 테스트 불가능한 구조.
- CDN `<script>`에 SRI(무결성 해시) 없음. qrcode-generator·jsQR은 수 KB라 vendoring(저장소에 포함)하는 편이 공급망·오프라인 양쪽에 낫다.
- CSV 내보내기에 수식 인젝션 가드 없음(`=`, `+` 등으로 시작하는 셀). 본인 데이터라 위험은 낮지만 한 줄이면 막는다.

### 4.4 보안 분석 (외부 개발자가 가장 먼저 지적할 영역)

문서가 위협 모델을 정직하게 선언("암호는 탈취돼도 무방한 편의용, 무단 등록·수정 방지 수준")한 점은 높이 산다. 그러나 선언된 전제보다 실제 노출 면이 넓은 지점들이 있다:

| # | 심각도 | 항목 | 내용 |
|---|---|---|---|
| S1 | **높음** | **초대코드·DB ID가 저장소에 평문 커밋** | `v2/wrangler.toml`에 `INVITE_CODE = "BH-56JQKGQT"`가 그대로 있다. 현재 private 저장소지만, 공개 전환·포크·화면 공유 한 번이면 가입 통제가 무력화된다. Cloudflare **secret**(`wrangler pages secret put`) 또는 대시보드 환경변수로 옮기고 커밋 이력에서 교체해야 함 |
| S2 | **높음** | **PIN 무차별 대입에 레이트 리밋 없음** | 유저코드는 **모든 인쇄 라벨의 KEY 앞 4자리로 공개**된다. 즉 라벨을 본 사람은 유저코드를 알고, 인증 엔드포인트에 스로틀이 없으므로 PIN 10,000개 전수 시도가 가능(수 분 내). 성공 시 수정·삭제·CSV 백업 탈취까지 가능해 "무단 등록·수정 방지"라는 선언된 목표 자체가 깨진다. 완화: Cloudflare 무료 플랜의 레이트 리밋 룰 1개를 `/api/*`에 적용하거나, D1에 실패 카운터 기반 백오프 |
| S3 | 중간 | 오프라인 크래킹 내성 없음 | `SHA-256(usercode:pin)` 단일 해시 — DB 유출 시 PIN 4자리는 즉시 역산. 위협 모델상 수용 가능하나, PBKDF2 몇천 회만 돌려도(Workers에서 가능) 비용 대비 이득이 큼. 복구키(80비트)는 해시 저장·1회용 회전 등 대조적으로 잘 설계됨 |
| S4 | 중간 | `/api/fetch` 프록시의 SSRF 가드가 문자열 기반 | 사설 IP·메타데이터 호스트를 **호스트명 문자열**로만 차단 — 공인 도메인이 사설 IP로 리졸브되는 DNS 리바인딩, 리다이렉트 후 내부 주소는 통과한다. Cloudflare Workers의 egress 특성상 현 배포에선 실위험이 낮지만, 코드가 다른 런타임으로 이식되면 즉시 구멍. 또한 인증 사용자에게 범용 fetch 프록시를 제공하는 셈이라 초대 모델 신뢰에 의존함(2MB·8초 제한은 적절) |
| S5 | 낮음 | PIN이 localStorage 평문 저장 | 선언된 모델과 일치하나, 공용 PC 사용 안내가 없음 |
| S6 | 낮음 | 초대코드 비교의 시차·시도 제한 없음 | signup 스팸으로 users 테이블 오염 가능(초대코드를 알 경우). 운영자 수동 정리로 수용 가능 |

### 4.5 운영·기여 인프라 (가장 큰 공백)

- **테스트 0, CI 0**: 자동채우기 파서(`parseBeanText`)와 라벨 레이아웃은 순수 함수라 단위 테스트하기 딱 좋은 형태인데 테스트가 없다. 지금까지는 "라이브 전 구간 수동 검증"(진행기록에 명시)으로 커버했지만, 외부 기여자는 회귀를 확인할 수단이 없다.
- **로컬 개발 절차 미문서화**: README의 운영 섹션은 배포만 다룬다. `npx wrangler pages dev v2/public` + 로컬 D1(`--local` + schema.sql/seed.sql 적용) 절차가 한 단락만 있어도 기여 진입 장벽이 크게 낮아진다. seed.sql(데모 계정)이 이미 있어서 재료는 갖춰져 있다.
- **LICENSE 없음**: private이라 당장은 무방하나, "외부 개발자" 시나리오에서는 법적으로 아무것도 할 수 없는 상태.
- 마이그레이션이 `migrate_*.sql` 파일 나열 방식 — 적용 여부 추적이 없어 새 환경 재현 시 실행 순서를 사람이 기억해야 한다. 파일명에 번호를 붙이고 README에 순서를 남기는 정도의 최소 조치 권장.

---

## 5. 개선 제안 (우선순위)

**P0 — 지금 (보안 실구멍)**
1. `INVITE_CODE`를 wrangler secret/대시보드 환경변수로 이전, 기존 코드 폐기·교체 (S1)
2. `/api/*`에 Cloudflare 레이트 리밋 룰(무료 1개) 적용 — 최소한 인증 실패 경로 (S2)

**P1 — 첫 실사용자 초대 전**
3. 라벨 디자인 설정·로고를 localStorage에 영속화 (U1)
4. 메모 입력란에 "QR 스캔한 누구나 볼 수 있음" 안내 추가, 장기적으로 비공개 메모 분리 (U2)
5. `pass_hash`를 PBKDF2/scrypt로 강화 — 기존 사용자는 로그인 시 재해시로 무중단 이전 가능 (S3)
6. CDN 스크립트 2종 vendoring 또는 SRI 부착 (4.3)

**P2 — 기여 받을 준비**
7. `parseBeanText`·라벨 레이아웃 단위 테스트 + GitHub Actions 1개
8. README에 로컬 개발 섹션 (wrangler pages dev + 로컬 D1 + seed)
9. `tools/make_label.py`를 v2 보딩패스 디자인과 동기화하거나 "v1 디자인 전용"으로 명시 (4.2)
10. DESIGN.md API 목록에 recover/fetch 반영, LICENSE 결정
11. `/api/fetch`에 리졸브 IP 검증 또는 리다이렉트 후 재검사 (S4, 이식 대비)

---

## 6. 결론

- **사용자로서**: 초대만 받으면 지금 바로 쓸 만하다. 등록 마찰 제거와 인쇄 전 QR 실검증은 이 도구의 존재 이유를 정확히 충족하며, 조회·덱 페이지는 취미 도구 이상의 만듦새다. 남은 체크리스트(님봇 실인쇄 → 폰 스캔 테스트)가 완료되면 핵심 루프는 닫힌다.
- **외부 개발자로서**: 읽는 데 반나절이면 충분한, 문서가 이끄는 모범적인 소규모 코드베이스다. 다만 기여 인프라(테스트·CI·로컬 가이드·라이선스)가 전무하고, 보안은 "선언한 위협 모델"과 "실제 노출 면" 사이의 간극 — 특히 라벨에 공개되는 유저코드 + 무제한 PIN 시도 조합(S2)과 커밋된 초대코드(S1) — 을 먼저 닫아야 한다. 이 두 가지는 반나절 작업으로 해결 가능하며, 그 후라면 이 프로젝트는 규모·비용·완성도의 균형이 매우 좋은 레퍼런스로 남을 것이다.
