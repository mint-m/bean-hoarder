# Bean-Hoarder 디자인 시스템

Bean-Hoarder의 시각 언어를 한곳에 모은 단일 기준이다. **토큰 값(색·서체·반경 등)의 단일 소스는
코드** — [apps/web/public/theme.css](apps/web/public/theme.css)의 `:root`다. 이 문서는 그 값들이
**무엇을 위한 것이고 어떻게 조합하는지 — 규칙과 근거**를 다룬다. 값을 바꾸려면 theme.css를,
규칙을 바꾸려면 이 문서를 고친다. 값을 여기 옮겨 적으면 낡으므로 하지 않는다.

> 설계 배경·아키텍처는 [README.md](README.md) "구조"와 [HOW_IT_WORKS.html](HOW_IT_WORKS.html),
> 저장소 구조는 [STRUCTURE.md](STRUCTURE.md), 작업 원칙·함정은 [CLAUDE.md](CLAUDE.md).

## 1. 아이덴티티 — 중립 크롬, 커피가 부여하는 색

- 화면 골격(크롬)은 **화이트·블랙 중립 + 포인트 컬러 하나(탠저린)**. 은유는 모바일 **보딩패스/월렛**.
- 개성·다양성은 크롬이 아니라 **원두 자체**에서 나온다 — 산지는 고정 시그니처 색, 향미는 무드 그라데이션(§3).
- **미니멀·문서형**: 그라데이션·드롭섀도·장식은 최소화하고 여백과 타이포로 정돈한다. 절제된 문서형
  레이아웃(Ollama류)을 참고하되, 그들이 배제한 "색"을 우리는 **커피에서 끌어온다**는 점이 우리의 차이다.
- 브랜드 accent(탠저린)는 한 화면에 드물게. **원두 맥락이 있는 화면(상세)에서는 accent 자리를 그 원두의
  산지 색이 대신한다** — 크롬은 물러나고 원두가 색을 갖는다.

### "AI가 만든 것 같다"를 부르는 것 → 그 대신 (이슈 #32의 핵심)

생성형 템플릿 특유의 인상은 대개 **장식으로 위계를 만들려는 시도**에서 온다. 우리는 반대로 간다 —
**위계는 타이포와 여백이 만들고, 색은 의미가 있을 때만 쓴다.**

| 부르는 것 | 왜 문제인가 | 그 대신 |
|---|---|---|
| 카드 상단 **풀블리드 컬러 스트립/바** | 의미 없이 넓은 면적을 색으로 덮는 전형적 템플릿 장치 | 색은 **작고 의도적인 마크 하나**(산지 스와치·닷)로 |
| 액센트색 **대문자+넓은 레터스페이싱 eyebrow** 남발 | 모든 줄이 "강조"라 아무것도 강조되지 않음 | 중립색(`--sub`)·좁은 자간. 강조는 크기·굵기·여백으로 |
| 액센트 **배지·알약**으로 정보 표시(D+N 등) | 포인트 컬러를 장식으로 소진 | 헤어라인 알약 또는 그냥 텍스트 |
| 나란히 놓인 **여러 개의 액센트 링크·버튼** | 무엇이 주 동선인지 사라짐 | 주 동선 하나만 강조, 보조는 중립 보더·밑줄 |
| 어디나 **큰 라운드 + 강한 그림자** | 몰개성한 "카드 더미" | 헤어라인 + 여백 우선, 그림자는 절제 |

**한 화면에서 accent가 등장하는 횟수를 센다.** 진짜 동선(주 버튼·본문 링크·포커스)에만 남기고 나머지는 덜어낸다.

## 2. 색 시스템

값은 theme.css `:root`(+ 다크 오버라이드)에 있다. 여기서는 **역할과 규칙**만 다룬다.

**중립 베이스**
- `--bg` / `--surface` / `--surface-2` — 페이지 배경 · 카드 표면 · 보조 표면.
- `--ink` — 본문·제목.
- `--sub` — 보조 텍스트. **가시성 기준**: 라이트 모드에서 `--bg`·`--surface` 양쪽 위 본문 크기 대비
  **WCAG AA(≥4.5:1)** 를 지킨다. (이슈 #32의 핵심이 이 대비였다.)
- `--line` — 1px 경계.

**브랜드 · 강조**
- `--accent`(탠저린) + `--accent-hover` / `--accent-ink` — 유일한 브랜드 색. CTA·링크·포커스. **스코프**:
  원두 맥락이 없는 크롬(랜딩·랩·덱 기본값). 원두 상세에서는 §3의 산지 색으로 로컬 대체된다.
- `--accent-soft` / `--focus-ring` — accent에서 파생된 은은한 배경·포커스 링.

**상태색**
- `--ok` / `--bad` — 성공·오류 전용. 마케팅 배지가 아니다 — 남용하지 않는다.

**파생(color-mix)**
- `--fill-muted` · `--hover` 등은 `--sub`/`--accent`에서 유도되어 라이트·다크에 자동 적응한다. **새 색을
  하드코딩하기 전에 기존 토큰·파생으로 표현되는지 먼저 본다.**

**다크 모드**
- `:root[data-theme="dark"]` 오버라이드(OS 설정이 아니라 사용자의 선택 — 아래 "테마" 참고).
  **라이트를 먼저 확정하고 다크는 대비를 맞춰 파생**한다.

## 3. 커피 컬러 — 국가(모노) · 향미(그라데이션) — *화면 전용*

이 문서가 커피 컬러 규칙의 **단일 소스**다. 엔진(코드)은
[apps/web/src/lib/coffee-color.ts](apps/web/src/lib/coffee-color.ts)가 이 규칙을 구현한다.

**경계 원칙 (먼저 못 박는다)**
- **인쇄물은 색을 지원하지 않는다.** 감열 인쇄는 흑백 또는 레드+블랙 2도(`#e8341c`, 기본 흑백) —
  [packages/label](packages/label)의 인쇄 모델이다. 커피 컬러는 **화면 전용**이며, 라벨과 식별성은
  **색이 없어도 성립**해야 한다(색은 거들 뿐, 정보를 지지 않는다).
- **커피 컬러가 나타나는 곳은 원두 맥락 화면 — 상세(조회 카드)와 덱**이다. 랩은 정돈 대상일 뿐 커피
  컬러를 얹지 않는다(라벨 프리뷰가 이미 결과물을 보여준다).

### 국가 — 시그니처 모노컬러
- 산지(ORIGIN) 문자열 → **결정론적 단색 하나**. "산지마다 늘 같은 색."
- **근거**: DB에 색을 저장하지 않고 문자열에서 생성한다 → 새 산지가 와도 매핑표 유지가 필요 없다.
- **구현**: **OKLCH** — hue만 산지마다 다르고 L·C는 테마 공통으로 고정해, 어떤 hue든 지각적 밝기·채도가
  고른 "한 세트" 팔레트가 나온다. 자주 쓰는 산지(에티오피아·콜롬비아·케냐·브라질·과테말라·코스타리카·
  인도네시아·파나마·르완다 등)는 **큐레이션 hue**(손으로 고른 색), 그 밖은 해시 폴백.
- **적용**: 상세 카드 밴드의 **작은 스와치(10px 닷)**와 덱 피크 밴드의 **산지 닷** — 등장 자리는 이 둘뿐.
  **풀블리드 상단 스트립은 쓰지 않는다** — 위 anti-AI 표 참고. 색은 정보를 지지 않으므로(스와치가 없어도
  산지명이 헤드라인에 있다) 장식이 아니라 **보조 식별 신호**다.

### 향미 — 무드 그라데이션
- 테이스팅 노트(`TASTING_NOTE`, 콤마/`·` 구분 자유입력)를 분해 → **향미 계열**로 판정 → 검출된 계열 색을
  조합한 `linear-gradient`.
- **향미 계열 → 무드색 방향** (기준표):

  | 계열 | 예시 키워드 | 무드색 |
  |---|---|---|
  | 베리·붉은과일 | berry, strawberry, cherry, grape | 레드·마젠타 |
  | 시트러스 | citrus, lemon, orange, bergamot | 오렌지·옐로 |
  | 열대과일 | tropical, mango, pineapple, passion | 앰버·골드 |
  | 플로럴 | floral, jasmine, rose, lavender | 핑크·바이올렛 |
  | 초콜릿·카카오 | chocolate, cocoa | 딥 브라운 |
  | 넛티·캐러멜 | nutty, almond, caramel, brown sugar | 탠·앰버 |
  | 스파이스·허브 | spice, cinnamon, herbal, black tea | 웜 브라운·올리브 |
  | 스톤·그린 | green apple, peach, apricot, melon | 옐로그린 |
  | 와이니·발효 | winey, boozy, fermented | 딥 퍼플레드 |
  | (매칭 없음) | — | 중립 커피 그라데이션(웜 브라운) |

- **조합 규칙**: 검출된 계열의 대표색을 등장 순서대로 2~3색까지 그라데이션으로. 단일 계열이면 그 색의
  명도 두 단계로. 매칭이 없으면 중립 커피 그라데이션.
- **제약**: **저채도·저알파**로 얹어 본문 가시성(#32)을 해치지 않는다 — 텍스트 뒤 은은한 워시로,
  콘텐츠 대비를 항상 우선한다.
- **적용**: 그라데이션은 **구조적 자리**에만 얹는다 — 상세 카드의 아이덴티티 밴드(§7)와 덱 카드의
  피크 밴드(§7). 본문·스텁 영역에는 얹지 않는다.

> 구현 노트: 국가·향미 엔진 모두 [apps/web/src/lib/coffee-color.ts](apps/web/src/lib/coffee-color.ts)
> (`originSignature` · `flavorGradient`). 규칙의 단일 소스는 이 문서 — 계열표를 바꾸면 코드도 함께 고친다.

## 4. 타이포그래피

- `--display`(**Space Grotesk**) — 워드마크·헤드라인(산지명)·히어로. 700.
- `--sans`(**Pretendard Variable**, 한글 우선) — 본문·버튼·대부분의 UI.
- `--mono`(SF Mono 계열) — KEY·코드·날짜·스펙 라벨(`dt`).
- **크기 감각**: 헤드라인 `clamp(32–42px)`, 본문 14px, 보조 11.5–13px, 마이크로 10–11px(대문자+레터스페이싱).
  본문 라인하이트 1.55.
- **원칙**: 디스플레이 서체는 산지명·브랜드에만. 본문 이하는 시스템 한글 서체 그대로 둔다. 과한 웨이트·
  이탤릭은 지양(테이스팅 노트만 이탤릭 허용).

## 5. 여백 · 레이아웃

- **여백 스케일**(theme.css `--sp-1`…`--sp-7` = 4·8·12·16·24·32·48px, 신설). 컴포넌트 간격은 이 스케일에서
  고른다. 기존 인라인 px 잔재는 점진 이관(한 번에 치환하지 않는다).
- **컨테이너**: 상세·조회는 최대 430px, 랩([apps/lab](apps/lab))은 최대 640px — 둘 다 **모바일 우선 단일 컬럼**이다.
  랩도 2컬럼을 버렸다: 한 화면에 한 맥락만 두면 열을 나눌 이유가 없다(§7).
- **카드 리듬**: `.card`(`--radius` · 1px 라인 · 그림자). 섹션은 여백으로 가르고 장식 구분선은 지양.

## 6. 반경 · 입체 · 모션

- **반경**: `--radius` 10px(카드) / `--radius-sm` 7px(버튼·인풋) / `--radius-pill` 999px(알약·닷·배지).
  구 16/9px에서 줄였다 — 과한 둥글기는 몰개성한 "둥글둥글 카드" 인상(anti-AI §1)을 만든다.
  임의 반경은 쓰지 않는다 — 인라인 값 대신 반드시 토큰으로.
- **입체**: **그림자만**으로 띄운다(`--shadow`/`--shadow-hover`). 배경 그라데이션·과한 보더로 띄우지 않는다.
  (예외: §3 향미 그라데이션 — 상세 밴드·덱 피크 밴드 한정, 저알파. 덱은 밴드 아래 12px을 본문 색으로
  빠르게 풀어 띠가 덧댄 것처럼 잘려 보이지 않게 한다.)
- **모션**: `--t`(0.15s ease) 공통. 색·보더·배경 전이에만. 장식 애니메이션은 지양.

## 7. 컴포넌트 패턴

- **버튼**: 기본(중립 보더) · `.primary`(ink 배경, hover 시 accent) · `.danger`. 반경 `--radius-sm`.
- **인풋**: 1px `--line`, 포커스 시 accent 보더 + focus-ring.
- **칩(`ul.chips`)**: 알약, 1px `--ink` 보더 — 테이스팅 노트 등. (향미 색은 §3 후속에서 얹을 수 있으나 기본은 중립.)
- **보딩패스 카드(`.pass`)** — 상세·조회의 핵심 컴포넌트. 항공권의 3단 구조를 그대로 쓴다:
  ① **아이덴티티 밴드**(`.pass-band`) — 향미 그라데이션 배경 + 산지 스와치 + 로스터리 + 헤드라인 + 서브라인,
  ② **본문** — 노트 칩 + **티켓 정보 행**(`.ticket-row`: RSTD/PKGD/NET/ROAST PT를 mono 컬럼으로 — 게이트/좌석
  자리) + 부가 스펙 한 줄(`.specline`), ③ 절취선(`.perf`) → **스텁** — KEY(티켓 번호)·`D+N`·원본 링크·메모.
  브랜드는 카드 밖 페이지 푸터가 맡는다. 렌더는 [apps/web/src/viewer.ts](apps/web/src/viewer.ts).
- **월렛 카드(덱)** — **피크 밴드**(`.w-band`)가 1급 설계 단위: 카드 높이를 고정하고 스택시 보이는 상단
  띠에 향미 그라데이션 + 산지 닷 + 헤드라인 + 경과일 + 로스터리·KEY를 모두 싣는다. 본문(노트·날짜)은
  카드를 뽑았을 때만. 렌더는 [apps/web/src/deck.ts](apps/web/src/deck.ts).
- **랩(화면 전환)** — 단일 컬럼(최대 640px)이되 **한 번에 한 화면만** 보인다: 입력 / QR 발급 /
  라벨 도안 / 목록이 서로를 가리지 않는다(브라우저 뒤로가기가 이전 화면으로 돌아간다).
  여러 카드가 한 페이지에 공존하면 "지금 무엇을 하는 중인지"가 흐려지기 때문이다.
  서비스가 책임지는 산출물이 "라벨 도안"이 아니라 **인쇄되는 QR과 그 QR이 도착하는 상세 페이지**라서
  화면 순서가 이렇게 정해진다:
  ① **입력** — [IntakeCard](apps/lab/src/components/IntakeCard.tsx)의 **입력 칸 하나**에서 시작한다.
  링크를 넣으면 그 페이지를 가져와 읽고, 페이지 내용을 붙여넣으면 그 본문을 읽는다 — 사용자에겐 둘 다
  "정보가 있는 것을 붙여넣는" 같은 행동이라 칸을 나누지 않는다. 링크라면 **가져오기 전에** 같은 상품
  페이지로 등록한 원두가 있는지 먼저 확인해 헛된 요청을 막는다.
  이어 [ReviewStepper](apps/lab/src/components/ReviewStepper.tsx)로 넘긴다. 스텝은 등록 필수 순서(로스터리·산지 →
  가공·품종 → 날짜 → 표현 → 상세)로 **한 번에 하나만 열리고**, 각 스텝은 **건너뛸 수 있다**.
  칸마다 추천 칩을 붙여 타이핑을 줄이고(로스팅일은 D+N을 함께 보여준다), AI가 채운 칸에는 "확인" 표시를 남긴다.
  ② **QR 발급** — [QrCard](apps/lab/src/components/QrCard.tsx). 등록으로 KEY가 확정된 뒤에만 QR을 내보낸다
  (확정 전 QR은 없는 곳을 가리킨다). 인쇄 크기는 203dpi 도트의 정수배로만 제시하고, 화면에는 dpi·디코더
  대신 **결과만**(`✓ 인쇄해도 스캔됩니다`) 말한다. 버튼은 목적별로 묶는다 — 주소(복사) / 인쇄물(크기·이미지).
  ③ **유지관리** — 목록·수정·삭제·CSV 백업. 덱 카드에서도 "정보 수정"으로 이 화면의 편집 상태에 바로 들어온다.
  **라벨 이미지는 접힌 선택 패널**([LabelPanel](apps/lab/src/components/LabelPanel.tsx))로 내린다 —
  쓰는 사람마다 원하는 양식이 다르고 프린터 호환도 보장할 수 없기 때문이다. 기능은 그대로 남긴다.
  스타일은 theme.css 토큰 + [apps/web/public/lab.css](apps/web/public/lab.css).
- **복사 버튼** — 복사 결과는 **버튼 자신이 말한다**(1.5초 `✓ 복사완료`). 멀리 떨어진 상태줄에만 띄우면
  성공했는지 몰라 같은 버튼을 여러 번 누르게 된다. 구현은 `CopyButton`([FormBits](apps/lab/src/components/FormBits.tsx)).
- **설정 서랍** — 테마·AI 키처럼 **지금 할 일과 무관한 것**은 우측 상단의 작은 버튼 뒤로 치운다
  ([SettingsMenu](apps/lab/src/components/SettingsMenu.tsx)). 본 화면에 늘 펼쳐 두면 주 동선이 흐려진다.

### 필요할 때만 보여준다 (점진적 노출)

우회로·예외 수단을 평소에 펼쳐 두면 그것만으로 화면이 무거워지고, 무엇이 주 동선인지 사라진다.
그래서 **평소엔 없다가 필요해진 순간 나타나게** 한다.

| 무엇 | 언제 나타나는가 |
|---|---|
| 추천 칩의 뒷부분 | `+ N개 더`를 누를 때 — 평소엔 자주 쓰는 것만 |
| 라벨 도안 | QR 화면에서 "라벨 도안까지 만들기"를 고를 때 |
| AI 키·테마 | 설정 서랍을 열 때 |
| QR 내보내기 | 등록으로 KEY가 확정된 뒤 — 그전 QR은 없는 곳을 가리킨다 |

더 나은 해법이 있으면 "나타나게 하는" 것보다 **아예 없애는** 쪽이 낫다. 인테이크의 텍스트 붙여넣기가
그랬다 — 처음엔 링크가 실패한 뒤에만 열리는 우회로였는데, 링크와 본문을 **한 칸에서 받게** 하니
분기 자체가 사라졌다(입력이 링크처럼 생겼으면 페이지를 가져오고, 아니면 그 본문을 읽는다).

짝이 되는 규칙: **실패 안내는 그 실패를 해결할 UI 바로 옆에 둔다.** 버튼을 눌렀는데 저 멀리 화면
위쪽에 메시지가 뜨면 사용자는 "아무 일도 안 일어났다"고 읽고 같은 버튼을 다시 누른다.

### 테마

**기본은 라이트 고정**이다 — OS 다크 설정을 따라가지 않는다. 다크는 설정에서 켜는 선택지이며
`:root[data-theme="dark"]`로만 적용된다(theme.css). 각 HTML의 head에 있는 인라인 스크립트가
저장값을 첫 페인트 전에 붙여 깜빡임을 막는다 — 블로킹이어야 해서 세 진입점에 같은 사본을 둔다.

## 8. 해야 할 것 · 하지 말 것

**Do**
- 크롬은 중립으로, 색은 원두(산지·향미)에서.
- 값은 theme.css 토큰으로. 새 하드코딩 전에 기존 토큰·파생으로 되는지 확인.
- 반경은 3단계, 입체는 그림자, 포인트 컬러는 화면당 드물게.
- 헤드라인 조합은 [packages/schema/src/headline.ts](packages/schema/src/headline.ts)를 단일 소스로
  (카드·라벨이 같은 제목을 쓴다).
- 커피 컬러는 **라벨엔 색이 없다는 전제**로 설계 — 색이 없어도 읽히게.

**Don't**
- **카드 상단 풀블리드 컬러 스트립·바를 만들기** (산지 색은 작은 스와치로).
- **accent를 장식으로 쓰기** — eyebrow·배지·알약·나란한 링크 여러 개. accent는 진짜 동선에만.
- 크롬에 새 브랜드색·그라데이션·드롭섀도를 남발.
- 라벨에 화면 색(산지·향미)을 가정.
- 텍스트 대비를 해치는 배경(향미 그라데이션도 저알파 유지).
- theme.css 밖에 색·반경 하드코딩(불가피하면 이유를 주석으로).
- 카드 반경(16)·컨트롤 반경(9)·알약(999)을 서로 바꿔 쓰기.
